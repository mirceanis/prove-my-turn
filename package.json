{
  "name": "prove-my-turn",
  "private": true,
  "description": "Experiments in zk gaming",
  "author": "Mircea Nistor",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "start": "pnpm --filter zhuffle start",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "eslint --fix packages/*/src/**/*.ts",
    "format": "prettier --write --ignore-unknown packages/*/src/**/*"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.60.1",
    "@typescript-eslint/parser": "^8.60.1",
    "eslint": "^10.4.1",
    "prettier": "^3.8.3",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
